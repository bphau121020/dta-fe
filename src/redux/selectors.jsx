export const userSelector = (state) => state.user.value;
export const placeSelector = (state) => state.dashboard.place;
export const suggestPlaceSelector = (state) => state.dashboard.suggestPlace;
export const calSelector = (state) => state.dashboard.cal;
export const inputSelector = (state) => state.dashboard.input;
export const maxSelector = (state) => state.dashboard.max;